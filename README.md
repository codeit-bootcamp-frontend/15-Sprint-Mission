## 판다마켓 4

**🌐 배포 주소 : https://myungjiwoo-pandamarket.netlify.app/**

### 기본 요구사항

- [x] Github의 PR을 만들어서 미션을 제출한다.
- [x] 피그마 디자인에 맞게 페이지를 만든다.
- [x] UI 라이브러리를 사용하지 않고 진행한다.

### 체크 리스트 (기본)

**로그인**

- [x] 이메일 input에서 focus out할 때, 값이 없을 경우 input에 빨간 테두리와 아래에 "이메일을 입력해주세요."라는 빨간색 에러 메시지 표시
- [x] 이메일 input에서 focus out할 때, 이메일 형식에 맞지 않는 경우 input에 빨간 테두리와 아래에 "잘못된 이메일 형식입니다."라는 빨간색 에러 메시지 표시
- [x] 비밀번호 input에서 focus out할 때, 값이 없을 경우 아래에 "비밀번호를 입력해주세요."라는 에러 메시지 표시
- [x] 비밀번호 input에서 focus out할 때, 값이 8자 미만일 경우 아래에 "비밀번호를 8자 이상 입력해주세요."라는 에러 메시지 표시
- [x] input에 빈 값이 있거나 에러 메시지가 있으면 '로그인' 버튼을 비활성화
- [x] input에 유효한 값을 입력하면 '로그인' 버튼 활성화
- [x] 활성화된 '로그인' 버튼을 누르면 '/items'로 이동

**회원가입**

- [x] 이메일 input에서 focus out할 때, 값이 없을 경우 input에 빨간 테두리와 아래에 "이메일을 입력해주세요."라는 빨간색 에러 메시지 표시
- [x] 이메일 input에서 focus out할 때, 이메일 형식에 맞지 않는 경우 input에 빨간 테두리와 아래에 "잘못된 이메일 형식입니다."라는 빨간색 에러 메시지 표시
- [x] 닉네임 input에서 focus out할 때, 값이 없을 경우 input에 빨간 테두리와 아래에 "닉네임을 입력해주세요."라는 빨간색 에러 메시지 표시
- [x] 비밀번호 input에서 focus out할 때, 값이 없을 경우 아래에 "비밀번호를 입력해주세요."라는 에러 메시지 표시
- [x] 비밀번호 input에서 focus out할 때, 값이 8자 미만일 경우 아래에 "비밀번호를 8자 이상 입력해주세요."라는 에러 메시지 표시
- [x] 비밀번호 input과 비밀번호 확인 input의 값이 다른 경우, 비밀번호 확인 input 아래에 "비밀번호가 일치하지 않습니다."라는 에러 메시지 표시
- [x] input에 빈 값이 있거나 에러 메시지가 있으면 '회원가입' 버튼을 비활성화
- [x] input에 유효한 값을 입력하면 '회원가입' 버튼 활성화
- [x] 활성화된 '회원가입' 버튼을 누르면 '/signin'로 이동

### 체크 리스트 (심화)

- [x] 눈 모양 아이콘 클릭시 비밀번호의 문자열이 보이기도 하고, 가려짐
- [x] 비밀번호의 문자열이 가려질 때는 눈 모양 아이콘에는 사선이 그어져 있고, 비밀번호의 문자열이 보일 때는 사선이 없는 눈 모양 아이콘이 보이도록 함

### 지난 코드 리뷰

- [x] 여백 없이 이미지 추출
- [x] 화면의 최소 크기가 0px이라면, min-width 제거
- [x] 불필요한 css 우선순위 수정 (unsupportedScreen.css)
- [x] 디자인 시안과 동일하게 좌우 padding 값 수정 (indexPhone.css)
- [x] button type 추가
